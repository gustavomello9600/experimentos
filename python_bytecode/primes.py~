from itertools import count
from toolz import take, cons

def sieve(start=2):
    p = start
    numbers = count(p + 1)
    yield from cons(p, (sieve(n) for n in numbers if n % p > 0))

primes = take(10, sieve(start=2))
for p in primes:
    print(p)
